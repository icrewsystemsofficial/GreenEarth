<?php

/*
    USER HELPER

    This is a helper file, generated by Leonard.
    This is NOT laravel default. Whenever you need to statically define
    something, ALWAYS do it via helpers, so that those things
    can be changed in the future / overloaded without a hassle.
*/

namespace App\Helpers;


class UserHelper {
    // public static function userTypes() : object {

    //     // $userTypes = array(
    //     //     array(
    //     //         'user' => 'Basic user'
    //     //     )
    //     // )

    //     return (object) $userTypes;
    // }
}
